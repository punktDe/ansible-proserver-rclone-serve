rclone_serve:
  defaults:
    daemon_user: proserver
    rclone: >-
      {%- if ansible_system == 'Linux' -%}
        /usr/bin/rclone
      {%- else -%}
        /usr/local/bin/rclone
      {%- endif -%}
  config: {}
